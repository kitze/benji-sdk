{
  "name": "benji-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "clean": "pnpm -r clean",
    "generate": "pnpm --filter benji-sdk generate"
  },
  "engines": {
    "node": ">=20.19.0",
    "pnpm": ">=9"
  }
}
